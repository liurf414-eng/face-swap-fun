# 🚀 Vercel全新部署指南（自动部署配置）

## 📋 前置条件

- ✅ GitHub仓库已准备好：`liurf414-eng/face-swap-fun`
- ✅ 代码已推送到GitHub
- ✅ 有Vercel账号

---

## 🎯 步骤1：在Vercel中创建新项目

### 1.1 打开Vercel Dashboard

1. 访问：https://vercel.com/dashboard
2. 登录你的Vercel账号

### 1.2 创建新项目

1. 点击 **"Add New..."** 按钮（通常在右上角或项目列表顶部）
2. 选择 **"Project"**

### 1.3 导入GitHub仓库

1. 在导入页面，选择 **"Import Git Repository"**
2. 如果首次使用，点击 **"Connect GitHub"** 或 **"Authorize Vercel"**
3. 授权Vercel访问你的GitHub账号
4. 搜索并选择：`liurf414-eng/face-swap-fun`
5. 点击 **"Import"**

---

## ⚙️ 步骤2：配置项目设置

### 2.1 项目配置页面

导入后，会进入项目配置页面。配置以下设置：

#### Framework Preset
- 选择：**"Vite"** 或 **"Other"**

#### Root Directory
- 留空（如果项目在仓库根目录）
- 或填写：`./`（如果项目在子目录）

#### Build and Output Settings

**Build Command:**
```
npm run build
```

**Output Directory:**
```
dist
```

**Install Command:**
```
npm install --legacy-peer-deps
```

**Development Command:**
```
npm run dev
```

#### Environment Variables（如果需要）

如果项目需要环境变量，在这里添加：
- `VITE_VMODEL_API_KEY`（如果有）
- 其他必要的环境变量

### 2.2 确认配置

检查所有设置后，点击 **"Deploy"** 按钮

---

## 🔄 步骤3：验证自动部署

### 3.1 首次部署

1. 等待首次部署完成（通常1-3分钟）
2. 部署成功后，会显示部署URL
3. 点击URL访问网站，确认部署成功

### 3.2 测试自动部署

推送一个测试提交：

```bash
git commit --allow-empty -m "Test auto-deploy after new setup"
git push
```

然后：
1. 立即打开Vercel Dashboard
2. 进入项目的 **"Deployments"** 页面
3. 观察是否有新部署自动开始

**如果看到新部署自动开始** = ✅ 自动部署配置成功！

---

## 🔍 步骤4：验证Git集成

### 4.1 检查Git设置

1. Vercel Dashboard → 项目 → **Settings** → **Git**
2. 确认显示：
   - **Connected Git Repository**: `liurf414-eng/face-swap-fun`
   - **Production Branch**: `main`（或你的主分支名）
   - **Pull Request Comments**: 已启用
   - **deployment_status Events**: 已启用

### 4.2 检查GitHub App

1. GitHub → Settings → Applications → Installed GitHub Apps
2. 确认 **Vercel** 应用已安装
3. 确认权限包括仓库访问

---

## 🛠️ 步骤5：配置构建优化（可选但推荐）

### 5.1 检查Vercel配置

确认项目根目录有 `vercel.json` 文件：

```json
{
  "rewrites": [
    {
      "source": "/api/(.*)",
      "destination": "/api/$1"
    },
    {
      "source": "/(.*)",
      "destination": "/index.html"
    }
  ],
  "headers": [
    {
      "source": "/(.*)",
      "headers": [
        {
          "key": "X-Content-Type-Options",
          "value": "nosniff"
        },
        {
          "key": "X-Frame-Options",
          "value": "DENY"
        },
        {
          "key": "X-XSS-Protection",
          "value": "1; mode=block"
        },
        {
          "key": "Referrer-Policy",
          "value": "strict-origin-when-cross-origin"
        }
      ]
    }
  ]
}
```

### 5.2 检查package.json

确认 `package.json` 中有正确的构建脚本：

```json
{
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview"
  }
}
```

---

## 📊 步骤6：监控部署状态

### 6.1 部署历史

1. Vercel Dashboard → 项目 → **Deployments**
2. 查看所有部署历史
3. 每个部署显示：
   - 触发方式（Git Push / Manual）
   - 提交信息
   - 部署状态
   - 部署时间

### 6.2 部署日志

1. 点击任意部署
2. 查看 **"Build Logs"** 和 **"Function Logs"**
3. 确认没有错误

---

## 🎯 完整配置检查清单

### Vercel项目设置
- [ ] 项目已创建
- [ ] GitHub仓库已连接
- [ ] Framework Preset: Vite
- [ ] Build Command: `npm run build`
- [ ] Output Directory: `dist`
- [ ] Install Command: `npm install --legacy-peer-deps`
- [ ] 环境变量已配置（如果需要）

### Git集成
- [ ] Connected Git Repository: `liurf414-eng/face-swap-fun`
- [ ] Production Branch: `main`
- [ ] Pull Request Comments: 已启用
- [ ] deployment_status Events: 已启用

### GitHub App
- [ ] Vercel GitHub App已安装
- [ ] 有仓库访问权限
- [ ] 权限完整

### 自动部署测试
- [ ] 推送测试提交
- [ ] Vercel自动开始部署
- [ ] 部署成功

---

## 🚨 常见问题

### 问题1：部署失败

**可能原因：**
- Build Command错误
- 依赖安装失败
- 环境变量缺失

**解决方法：**
1. 查看部署日志
2. 检查错误信息
3. 修正配置后重新部署

### 问题2：自动部署不工作

**可能原因：**
- GitHub App未正确安装
- 仓库权限不足
- 分支配置错误

**解决方法：**
1. 检查Git设置
2. 重新连接Git仓库
3. 检查GitHub App权限

### 问题3：找不到仓库

**可能原因：**
- GitHub授权未完成
- 仓库是私有的，需要授权

**解决方法：**
1. 重新授权GitHub
2. 确认仓库可见性
3. 检查Vercel团队权限

---

## 📝 下一步

部署成功后：
1. ✅ 测试网站功能
2. ✅ 配置自定义域名（如果需要）
3. ✅ 设置环境变量（如果需要）
4. ✅ 监控部署状态

---

**完成以上步骤后，你的项目应该可以自动部署了！**

